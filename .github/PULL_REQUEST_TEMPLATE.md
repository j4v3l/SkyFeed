## Summary

<!-- What changed and why. Link related issues with Fixes #N / Refs #N. -->

## Test plan

- [ ] `make check` (or `go test ./...` + format/vet) passes locally
- [ ] New behavior covered by unit tests where practical
- [ ] Config/docs updated (`.env.example`, `docs/`, README) if env or ops changed
- [ ] No secrets, private IPs, guild IDs, or coordinates in the diff

## Notes for reviewers

<!-- Risk areas, rollout steps, Discord command schema bumps, migrations. -->
